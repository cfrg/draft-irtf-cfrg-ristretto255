[tool.poetry]
name = "ristretto-draft"
version = "0.0.0"
description = ""
authors = []

[tool.poetry.dependencies]
python = "^3.7"
xml2rfc = "^2.22.3"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
